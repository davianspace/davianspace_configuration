name: davianspace_configuration
description: Flexible configuration system for Dart and Flutter with multi-source providers, hierarchical settings, environment overrides, JSON support, and runtime reload.
version: 1.0.0
homepage: https://github.com/davianspace/davianspace_configuration
repository: https://github.com/davianspace/davianspace_configuration
issue_tracker: https://github.com/davianspace/davianspace_configuration/issues
license: MIT

topics:
  - configuration
  - app-settings
  - environment
  - flutter
  - architecture
  - config-management

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
